# Claude Code Notes

Follow the canonical repository guidance in `AGENTS.md`.

When changing pricing, preserve the per-1,000-token storage convention, cite
official provider documentation, update the pricing timestamp, and add exact
rate and estimator tests. Do not infer or silently substitute unknown models.

Before handoff, run every verification command in `AGENTS.md` and confirm that
no generated report or fixture exposes credential values.
